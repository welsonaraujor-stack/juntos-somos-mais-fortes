const botao =
document.getElementById("btnInstituicao");

botao.addEventListener("click", () => {

    alert(
        "Campanha cadastrada com sucesso!"
    );

    // Futuramente:
    // integração com banco de dados

});