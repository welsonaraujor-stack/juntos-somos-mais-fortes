const botao =
document.getElementById("btnGrupo");

botao.addEventListener("click", () => {

    alert(
        "Solicitação enviada com sucesso!"
    );

    // Futuro:
    // enviar dados para banco de dados

});